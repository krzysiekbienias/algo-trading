# Copy to quant_sandbox/.env (gitignored) or export in your shell.
# Paths are relative to the monorepo root unless absolute.

# Default Parquet root for load_bars() when lake_root is omitted.
QUANT_DATA_LAKE=data_lake_m1

# Optional comma-separated list for notebook examples.
# QUANT_SYMBOLS=NVDA,AAPL
